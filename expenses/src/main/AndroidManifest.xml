<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.antso.expenses"
    android:versionCode="1"
    android:versionName="@string/app_version">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <uses-sdk
        android:minSdkVersion="16">
    </uses-sdk>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".StartActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".transactions.TransactionListActivity"
            android:label="@string/title_default_transaction_list_activity" >
        </activity>
        <activity
            android:name=".transactions.TransactionEntryActivity"
            android:label="@string/title_activity_transaction_entry" >
        </activity>
        <activity
            android:name=".accounts.AccountEntryActivity"
            android:label="@string/title_activity_account_entry" >
        </activity>
        <activity
            android:name=".budgets.BudgetEntryActivity"
            android:label="@string/title_activity_budget_entry" >
        </activity>
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings" >
        </activity>
        <activity
            android:name=".DebugActivity"
            android:label="@string/title_activity_debug" >
            <!--<intent-filter>-->
                <!--<action android:name=".DebugActivity" />-->
                <!--<category android:name="android.intent.category.DEFAULT" />-->
            <!--</intent-filter>-->
        </activity>
    </application>

</manifest>
